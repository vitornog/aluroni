import styles from "./cardapio.module.scss";

export default function Cardapio(){
    return(
        <main>
            <nav className={styles.menu}>
                
            </nav>
        </main>
    )
}